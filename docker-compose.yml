version: '3.9'

services:
  mysql:
    image: mysql:8
    ports:
      - 3306:3306
#    volumes:
#      - ~/apps/mysql:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=db_cashier
      - MYSQL_USER=trian-user
      - MYSQL_PASSWORD=password
